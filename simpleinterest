#include <stdio.h>
int main()
{
 int p,r,t;
 float si;
 scanf("%d",&p);
 printf("rate:");
 scanf("%d",&r);
 printf("years:");
 scanf("%d",&t);
 si=(float)(p*r*t)/100;
 printf("simple interest: %.2f ",si);
 return 0;
}
